% INPUTS///////////////////////////////////////////////////////////////////
starting_point = [-4, 8];
route = [6, 1, 1, 3, 4, 5, 2];
% /////////////////////////////////////////////////////////////////////////

% SOLUTION/////////////////////////////////////////////////////////////////
D = [0, 1; 1, 0; 0, -1; -1, 0];
C = cumsum([starting_point; ...
    route.' .* [repmat(D, [floor(length(route) / 4), 1]); D(1 : mod(length(route), 4), :)]]);
% /////////////////////////////////////////////////////////////////////////